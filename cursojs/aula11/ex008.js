var vel = 75.9;
console.log(`A velocidade é de ${vel}`);
if (vel >= 60){ //Condição simples
    console.log(`Você ultrapassou o limite de velocidade (${vel + ' km/h'}). Foi gerada uma multa!`);
}
console.log(`Dirija sempre usando o cinto de segurança`);
